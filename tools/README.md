# tools/

Допоміжні скрипти (з’являться пізніше). Не частина рантайму на телефоні.

Приклади майбутніх задач:

- валідація `protocol/` проти schema і інваріантів з docs/06;
- перевірка, що кожен `id` з docs/07 існує в графі;
- пакування `protocol/` у бандл для ios/ і web/.

Поки каталог порожній за змістом — лише місце під інструменти.
