# graphs/

Машиночитаний протокол для клієнтів.

## Поточна редакція

`zero-echelon-core/` — зріз **S0–S5b** (`graphVersion`: `2026-09-13-s0-s5b`):

| Файл | Зміст |
| --- | --- |
| `manifest.json` | версія, `commercial: false`, що відкладено |
| `graph.json` | вузли Start → Disclaimer → Loc → Type → Role → A1…A6 → Call / Out / Cont |

Ідентифікатори й тексти збігаються з `docs/07`. Карта екранів — `docs/12` (`ui.screenId`).

Наступна редакція: Count / самодопомога і гілки S6–S12.
